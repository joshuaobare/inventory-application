extends layout 

block content 
    h1=title 

    p Do you really want to delete this #{item}?

    form(action="" method="POST")
        div.form-group
            input#produuctid.form-control(type="hidden", name= 'productid', required='true', value=product._id) 
            button.btn.btn-primary(type='submit') Delete