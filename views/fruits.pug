extends layout 

block content 
    h1= title 

    ul 
        each fruit in fruit_list 
            li 
                a(href=fruit.url) #{fruit.name}
                
        else 
            li There are no fruits